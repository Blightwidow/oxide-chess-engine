[project]
name = "oxide-nnue-trainer"
version = "0.1.0"
description = "PyTorch NNUE trainer for Oxide chess engine"
requires-python = ">=3.11,<3.14"
dependencies = [
    "torch>=2.0",
    "numpy>=1.24",
]
